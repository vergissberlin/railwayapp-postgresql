# Set this in the Railway dashboard as a generated secret
POSTGRES_PASSWORD=

# Optional: import a database dump on the first start of an empty volume.
# POSTGRES_INIT_DUMP_URL takes precedence if both are set. See README.md.
# POSTGRES_INIT_DUMP_URL=https://example.com/dumps/seed.sql.gz
# POSTGRES_INIT_DUMP_BASE64=
